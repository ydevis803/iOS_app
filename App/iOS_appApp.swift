import SwiftUI

/// Application entry point; presents ``ContentView`` in the main window scene.
@main
struct FreshTrackApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
